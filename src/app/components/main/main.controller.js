;(function() {
    'use strict';

    angular
        .module('main')
        .controller('MainController', MainController);

    /* ngInject */
    function MainController($scope, $state) {
      var ctrl = this;
    }
})();
